import { SharedConfiguration } from './shared-configuration';

describe('SharedConfiguration', () => {
  it('should create an instance', () => {
    expect(new SharedConfiguration()).toBeTruthy();
  });
});
